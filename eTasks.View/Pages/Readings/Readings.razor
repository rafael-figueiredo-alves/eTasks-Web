@page "/readings"
@inject IActionButtonVisibleService? ActionButtonVisibleService


<LayoutView Layout="@typeof(eTasks.View.Layouts.MainLayout)">
    <InfoCard Image="assets/UI/resources/Leituras.webp"
              Text="Registre suas leituras de forma simples e digital. O eTasks permite manter tracking das suas leituras, basta pressionar o botão Mais e adicionar um novo registro." />
</LayoutView>

@code {
    protected override void OnInitialized()
    {
        ActionButtonVisibleService!.Visible = true;
    }
}