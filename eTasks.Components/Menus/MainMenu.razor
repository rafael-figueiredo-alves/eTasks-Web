@inherits MainMenuBase

<Offcanvas OffcanvasID="MainMenu" isDarkMode="@IsDarkMode">
    <div class="offcanvas-header" style="margin-top: 5px; height: 40px;">
        <img class="user-select-none" src="@CloseButton" height="20" width="20" style="margin: 10px; margin-left: 5px; cursor: pointer;" data-bs-dismiss="offcanvas" />
        <div class="container" style="text-align: center">
            <h3 class="user-select-none" style="font-size: 28px; color: @Components.ColorPallete.GetColor(Cor.Primary, IsDarkMode); margin-top: 10px;">@(GetMenuText(MainMenuTextsEnum.Title))</h3>
        </div>
    </div>
    <div style="@($"border: 2px solid {Components.ColorPallete.GetColor(Cor.Primary, IsDarkMode)}; box-shadow: 2px 4px 4px rgba(100,100,100,0.5); height: 1px; margin-left: 5%; margin-right: 5%")" />

    <MainMenuItem IsDarkMode="@IsDarkMode" Selected="@InicioSelected" ItemName="@(GetMenuText(MainMenuTextsEnum.Home))" OnClick="async () => await MainMenuItemClick(MainMenuItemType.Home)" />
    <MainMenuItem IsDarkMode="@IsDarkMode" Selected="@TarefasSelected" ItemType="MainMenuItemType.Tasks" ItemName="@(GetMenuText(MainMenuTextsEnum.Tasks))" OnClick="async () => await MainMenuItemClick(MainMenuItemType.Tasks)" />
    <MainMenuItem IsDarkMode="@IsDarkMode" Selected="@MetasSelected" ItemType="MainMenuItemType.Goals" ItemName="@(GetMenuText(MainMenuTextsEnum.Goals))" OnClick="async () => await MainMenuItemClick(MainMenuItemType.Goals)" />
    <MainMenuItem IsDarkMode="@IsDarkMode" Selected="@ComprasSelected" ItemType="MainMenuItemType.Shopping" ItemName="@(GetMenuText(MainMenuTextsEnum.Shopping))" OnClick="async () => await MainMenuItemClick(MainMenuItemType.Shopping)" />
    <MainMenuItem IsDarkMode="@IsDarkMode" Selected="@LeiturasSelected" ItemType="MainMenuItemType.Readings" ItemName="@(GetMenuText(MainMenuTextsEnum.Readings))" OnClick="async () => await MainMenuItemClick(MainMenuItemType.Readings)" />
    <MainMenuItem IsDarkMode="@IsDarkMode" Selected="@AnotacoesSelected" ItemType="MainMenuItemType.Notes" ItemName="@(GetMenuText(MainMenuTextsEnum.Notes))" OnClick="async () => await MainMenuItemClick(MainMenuItemType.Notes)" />
    <MainMenuItem IsDarkMode="@IsDarkMode" Selected="@FinancasSelected" ItemType="MainMenuItemType.Finance" ItemName="@(GetMenuText(MainMenuTextsEnum.Finance))" OnClick="async () => await MainMenuItemClick(MainMenuItemType.Finance)" />
</Offcanvas>